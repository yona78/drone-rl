# Extensions & Plugin Development

## Extension Points

The SDK middleware system allows registering custom callbacks at five lifecycle hooks. This enables extending behavior without modifying core code.

## Future Extensions

- Custom reward functions via middleware
- Additional obstacle types
- Multi-agent scenarios
- Alternative exploration strategies (softmax, UCB)
- Sarsa and Expected Sarsa variants
